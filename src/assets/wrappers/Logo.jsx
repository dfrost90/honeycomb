import styled from 'styled-components';

const Wrapper = styled.div`
  .logo {
    display: block;
    margin: 0 auto;
    max-width: 200px;
  }

  .logo img {
    width: 100%;
  }
`;

export default Wrapper;
